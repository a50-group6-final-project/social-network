package weare.testing;

import org.openqa.selenium.WebDriver;

public class HomePage extends BaseWeArePage{
    public HomePage(WebDriver driver, String urlKey) {
        super(driver, urlKey);
    }
}
